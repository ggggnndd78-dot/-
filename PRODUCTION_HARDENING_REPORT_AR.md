# تقرير نسخة الإنتاج - Ghiyarak

تم تجهيز هذه النسخة كنسخة إنتاج مدمجة بعد مراجعة الربط بين الواجهات، الأدوار، الصلاحيات، لوحة العميل، لوحة التاجر، لوحة الورشة، ولوحة الإدارة.

## 1) السياسات والصلاحيات

- تم اعتماد سياسة RBAC + Permissions + Organization Approval.
- تم تعديل حماية الواجهات بحيث يستطيع مدير النظام/مدير العمليات الدخول للإشراف بدون أن يُمنع بسبب عدم امتلاكه مؤسسة تاجر أو ورشة.
- تم ربط صلاحيات الموظفين المباشرة المخزنة على مستوى المؤسسة داخل `OrganizationMemberPermission` مع حارس الصلاحيات في الباكند، بحيث لا يعتمد الحارس على صلاحيات الدور فقط.
- تم توسيع صلاحيات `admin_operations` في seed لتشمل مراجعة المدفوعات، القيود المحاسبية، إدارة الأدوار، بعض صلاحيات التاجر/المخزون/الطلبات/الفروع/الموظفين، وبعض صلاحيات الورشة.

## 2) لوحة العميل

- تم حماية مسار البحث عن القطع داخل لوحة العميل بدور العميل.
- صفحات العميل الأساسية متصلة بالسياسات: السلة، الطلبات، الإرجاع، النزاعات، المركبات، الصيانة، المحفظة، الدعم، المحادثات، والإشعارات.

## 3) لوحة التاجر

- تم إضافة endpoints توافقية في الباكند للوحة التاجر حتى لا تكون الواجهات مربوطة على مسارات غير موجودة:
  - `/merchant/dashboard/summary`
  - `/merchant/inventory`
  - `/merchant/branches`
  - `/merchant/reports`
  - `/merchant/finance/summary`
  - `/merchant/finance/ledger`
  - `/merchant/finance/statement`
  - `/merchant/payments`
  - `/merchant/settlements`
  - `/merchant/invoices`
  - `/merchant/notifications`
  - `/merchant/notifications/read`
  - `/merchant/notifications/read-all`
  - `/merchant/notifications/preferences`
- تم ربط مسارات الشحنات في الفرونت مع باكند التوصيل الحقيقي `/delivery` بدل المسارات القديمة `/merchant/shipments`.
- تم إضافة عملية إعادة جدولة الشحنة في الباكند: `/delivery/shipments/:id/reschedule`.

## 4) إدارة الموظفين والفروع للتاجر/المؤسسة

- تم تعديل مسارات الفرونت من `/organizations/:id/members` إلى مسارات الباكند الحقيقية `/organizations/:id/employees`.
- تم إضافة دعم `PUT` لتحديث صلاحيات الموظف لأن الواجهة كانت تستخدم `PUT` بينما الباكند كان يدعم `PATCH` فقط.
- تم إضافة عملية تحديث صلاحيات الفروع للموظف.
- تم إضافة حذف/إزالة الموظف بتحويل حالته إلى `REMOVED` بدل حذف فعلي من قاعدة البيانات.
- تم تعديل payload صلاحيات الموظف من `permissionCodes` إلى `permissions` ليتوافق مع DTO في الباكند.

## 5) القيود المحاسبية والسياسة المالية

- نظام القيود كان يحتوي أساسًا على توازن Debit/Credit وإلزامية تساوي المدين والدائن.
- تم تعزيز القيود المحاسبية:
  - منع إنشاء قيد استرداد بمبلغ صفر أو سالب.
  - منع الاسترداد أو التسوية إذا كان رصيد المؤسسة غير كافٍ.
  - دعم الخصم من الرصيد المعلق أو المتاح حسب نوع العملية.
  - التسوية تخصم من الرصيد المتاح أولًا.
  - الاسترداد يخصم من الرصيد المعلق أولًا، ثم المتاح إذا لزم.
  - حفظ lifetimeRefunded و lifetimeSettled بطريقة متناسقة.
- بقيت قيود الدفع، الاسترداد، والتسوية مرتبطة بـ idempotency key حتى لا يتم تكرار القيد لنفس العملية.

## 6) الفحوصات

تم تشغيل فحص الباكند الثابت:

```bash
node backend/scripts/qa/static-check.js
```

والنتيجة:

```text
Static QA passed. Required modules, schema mappings and core files exist.
```

## 7) ملاحظات تشغيل مهمة

بيئة العمل هنا لا تحتوي Flutter SDK ولا `node_modules`، لذلك لم يتم تشغيل:

```bash
flutter analyze
npm run build
```

بعد فك الضغط على جهازك شغل الأوامر التالية:

### Backend

```powershell
cd backend
npm install
npx prisma generate
npx prisma db push
npx prisma db seed
npm run build
npm run start:dev
```

### Frontend

```powershell
cd frontend
flutter clean
flutter pub get
flutter analyze
flutter run -d windows
```

## 8) نقاط إنتاجية متبقية اختيارية

هذه النسخة أصبحت أقوى ومربوطة أكثر، لكن لكي تصبح إنتاجية 100% قبل النشر الحقيقي يفضل عمل اختبار يدوي لكل سيناريو:

- تسجيل عميل جديد ثم طلب منتج.
- تسجيل تاجر ورفع وثائق ثم اعتماد من الإدارة.
- إضافة منتج وربطه بمخزون.
- إنشاء طلب ثم تغيير حالته حتى التسليم.
- تجربة الدفع، الاسترداد، والتسوية.
- تجربة موظف تاجر بصلاحيات محددة وفروع محددة.
- تجربة مدير عمليات يدخل للإشراف على لوحات المزودين.

