# تقرير إصلاح السايدبار، الرجوع، والتجاوب

## الملخص
تم ضبط هيكل الواجهات المشتركة في غيارك حتى تكون القائمة الجانبية موحدة لكل الأدوار، قابلة للفتح والإغلاق بزر ثلاث نقاط، وتنتقل تلقائيًا حسب اتجاه اللغة:

- العربية RTL: القائمة تبدأ من اليمين.
- الإنجليزية LTR: القائمة تبدأ من اليسار.

## الملفات المعدلة

- `frontend/lib/shared/layout/dashboard_shell.dart`
- `frontend/lib/shared/widgets/app_scaffold.dart`
- `frontend/lib/core/platform/platform_layout.dart`
- `frontend/lib/features/admin/presentation/pages/admin_settings_page.dart`

## ما تم إصلاحه

### 1. زر فتح وإغلاق السايدبار
تم إضافة إطار موحد باسم `UnifiedSidebarFrame` يدير حالة السايدبار:

- عند فتح القائمة يظهر زر ثلاث نقاط داخل رأس القائمة لإغلاقها.
- عند إغلاق القائمة يظهر زر ثلاث نقاط عائم على جهة البداية حسب اللغة لإعادة فتحها.
- لا يتم إنشاء قوائم مختلفة لكل لوحة.

### 2. توحيد الاتجاه حسب اللغة
القائمة تعتمد على `Directionality`:

- RTL: السايدبار في اليمين.
- LTR: السايدبار في اليسار.

### 3. زر الرجوع
تم إضافة زر رجوع في الشريط العلوي للصفحات الداخلية:

- إذا كان يوجد مسار سابق يستخدم `maybePop`.
- إذا لم يوجد مسار سابق يرجع للصفحة الرئيسية المناسبة للدور.

### 4. تجاوب الشاشة عند التصغير
تم تعديل قرار `PlatformLayout.isDesktopShell` بحيث لا يتم إجبار نافذة Windows الضيقة على شكل desktop. عندما يقل العرض عن 1000px يتحول التخطيط إلى shell مضغوط لتجنب overflow.

### 5. إصلاح أخطاء ListTile في إعدادات الإدارة
تم تقييد عرض أيقونات `ListTile` داخل صفحة إعدادات الإدارة حتى لا تستهلك كامل عرض العنصر عند تضييق النافذة.

### 6. منع مشاكل عرض العناصر داخل المحتوى
تم جعل محتوى الصفحات يأخذ عرضًا محددًا وآمنًا داخل `AppScaffold` و `DashboardShell` بدل ترك بعض العناصر بعرض غير واضح عند التصغير.

## فحوصات تم تنفيذها هنا
- فحص توازن الأقواس للملفات المعدلة.
- التأكد من عدم استخدام أسماء Routes غير موجودة.
- التأكد من وجود إصلاحات الريبو السابقة الخاصة بعمليات التاجر.

## فحوصات يجب تشغيلها محليًا

```powershell
cd frontend
flutter clean
flutter pub get
dart format lib
flutter analyze
flutter run -d windows
```

## ملاحظة
لم يتم تشغيل Flutter داخل هذه البيئة لأن Flutter SDK غير مثبت هنا، لذلك يجب تشغيل الفحص النهائي على جهاز التطوير.
