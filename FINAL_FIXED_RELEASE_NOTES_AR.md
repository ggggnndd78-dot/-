# Ghiyarak v2.1 — النسخة المصححة النهائية

تم تعديل النسخة بناءً على فحص العمليات غير المكتملة وغير المنطقية في لوحة الإدارة، الاعتمادات، التسجيل، الترجمة، الإعدادات، وقنوات الإشعار.

## أهم الإصلاحات

- إصلاح انهيار واجهة التسجيل الناتج عن `ListTile.trailing` في كروت رفع الوثائق.
- إعادة تصميم كروت رفع الوثائق بحيث تعمل بدون أخطاء Layout على Windows/Desktop.
- دعم فتح مستندات الاعتمادات داخل لوحة الإدارة كصور أو PDF.
- دعم عرض الملفات من Base64 أو من رابط مباشر `file_url`.
- إصلاح مسارات التوجيه بعد تسجيل الدخول لتطابق مسارات Flutter الفعلية.
- إضافة دعم تسجيل نوع `WAREHOUSE` ضمن التسجيل والمسارات والأدوار.
- إصلاح اسم العميل بحيث لا يتم استخدام البريد الإلكتروني كاسم عرض افتراضي.
- إصلاح صلاحيات مراجعة الاعتمادات لتقبل الأدوار الإدارية أو الصلاحيات المناسبة.
- تفعيل إرسال إشعار الموافقة/الرفض عبر البريد الإلكتروني حسب قناة الإشعار المختارة.
- دعم إعدادات SMTP بصيغة `SMTP_*` بجانب صيغة `EMAIL_*` من ملف `.env`.
- إبقاء رسائل SMS للإشعارات العامة معطلة افتراضيًا لتجنب استخدام قناة OTP في رسائل نصية عادية، ويمكن تفعيلها عبر `NOTIFICATION_SMS_ENABLED=true` بعد تجهيز قناة SMS مناسبة.
- إصلاح صفحة إعدادات الإدارة لتعديل الإعدادات وتبديل Feature Flags من الواجهة.
- إصلاح صفحة كتالوج الترجمة لإضافة وتعديل مفاتيح الترجمة من لوحة الإدارة.
- إضافة مفاتيح ترجمة عربية وإنجليزية ناقصة للأجزاء الجديدة.
- إصلاح تكرار غير منطقي في `admin.service.ts` كان قد يسبب خطأ TypeScript.
- تثبيت إصدار PDF Viewer بدل استخدام `any`.
- تحديث `.env.example` بدون أي أسرار حقيقية.

## ملاحظات مهمة

- لا يتم تضمين أي كلمة مرور SMTP حقيقية داخل الكود أو ملفات المثال.
- ضع بيانات البريد في `backend/.env` محليًا فقط.
- يفضل تغيير/تدوير App Password الذي تم مشاركته في المحادثة قبل استخدامه في الإنتاج.
- تم تشغيل فحص Static QA للباكند ونجح.
- لم يتم تشغيل Flutter build داخل هذه البيئة لأن Flutter/Dart غير مثبتين هنا.
