# تقرير الدمج النهائي — Ghiyarak

تم اعتماد مجلد `ghiyarak` الكامل كنسخة أساس لأنه يحتوي على الباكند والفرونت والوثائق ومراحل الإدارة والاعتمادات والترجمة.

## ما تم دمجه

- دمج الملفات الفريدة من `lib.zip` داخل `frontend/lib`.
- دمج الملفات الفريدة من `lib2.zip` داخل `frontend/lib`.
- الحفاظ على ملفات النسخة الأساسية عند وجود تعارض؛ لأن النسخة الأساسية تحتوي إصلاحات الإدارة، الاعتمادات، الترجمة، صلاحيات الوصول، ومعاينة المستندات.
- إضافة أسماء المسارات الناقصة في `RouteNames` حتى لا تتعطل الملفات المدمجة التي تستخدم مسارات قديمة أو إضافية.
- إضافة نقاط API الناقصة في `ApiEndpoints` حتى لا تظهر أخطاء مراجع API غير معرفة داخل الملفات المدمجة.
- دمج تحسين مهم من `lib2`: عند انتهاء أو تلف التوكن يتم تنظيف الجلسة محليًا وإشعار `AuthController` لإرجاع المستخدم لحالة تسجيل الدخول بدل بقاء التطبيق في حالة غير صحيحة.
- الحفاظ على دعم جلسة الزائر `Guest` ورؤوس اللغة `Accept-Language / X-Locale` في `AuthInterceptor`.

## إصلاحات موجودة ومحفوظة في النسخة النهائية

- عمليات إدارة المستخدمين في لوحة الإدارة:
  - عرض المستخدمين.
  - إضافة مستخدم.
  - تعديل مستخدم.
  - حظر/تفعيل مستخدم.
  - حذف آمن للمستخدم مع المحافظة على السجلات المرتبطة.
- عمليات الاعتمادات:
  - عرض طلبات الاعتماد.
  - الموافقة/الرفض/طلب مستندات/تعليق الحساب.
  - معاينة المستندات داخل لوحة الإدارة للصور وملفات PDF عند توفر Base64 أو رابط مباشر.
- الترجمة واللغة:
  - تم الحفاظ على نظام `AppLocalizations` واتجاه RTL/LTR بدل الرجوع لنسخة عربية ثابتة فقط.
- صلاحيات الوصول:
  - تم الحفاظ على `_RequireAccess` وحماية مسارات الإدارة والمزودين والدعم.

## الفحوصات التي تمت داخل الحزمة

- فحص وجود كل imports الداخلية في Flutter: لا توجد imports مفقودة.
- فحص كل استخدامات `RouteNames`: لا توجد أسماء مسارات مفقودة.
- فحص كل استخدامات `ApiEndpoints`: لا توجد نقاط API مفقودة.
- فحص توازن الأقواس في ملفات Dart: لا توجد ملفات بكسر واضح في الأقواس.
- فحص imports النسبية في NestJS/TypeScript: لا توجد imports نسبية مفقودة.

## ملاحظة مهمة

لم يتم تشغيل `flutter analyze` أو `flutter build` داخل بيئة الدمج الحالية لأن Flutter SDK غير مثبت هنا. بعد تنزيل النسخة على جهازك شغل الأوامر التالية للتأكيد النهائي:

```powershell
cd frontend
flutter clean
flutter pub get
flutter analyze
flutter run -d windows
```

وللباكند:

```powershell
cd backend
npm install
npx prisma generate
npm run build
npm run start:dev
```

## تحديث لوحة التاجر
بعد المراجعة تم ربط لوحة التاجر الكاملة من `features/merchant_market` داخل الراوتر، وتفعيل مسارات المنتجات، الطلبات، المخزون، الفروع، الفريق، الصلاحيات، الشحنات، المالية، الإشعارات، الإعدادات، والتوثيق.
